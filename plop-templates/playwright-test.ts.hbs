import { test, expect } from '@playwright/test';

test.describe('{{pascalCase name}} Component', () => {
  test('should render correctly', async ({ page }) => {
    // Replace this with actual component rendering logic
    await page.goto('http://localhost:3000');

    // Example selector — adjust based on your component
    const component = page.locator('[data-testid="{{dashCase name}}"]');
    await expect(component).toBeVisible();
  });
});
